# Adaptive Breakout EA – Dashboard Frontend

Frontend scaffold (currently placeholder).

- Intended to talk to `dashboards/backend` FastAPI API.
- You can implement React/Vue/Svelte here later.

For now, `npm start` just prints a placeholder message.
