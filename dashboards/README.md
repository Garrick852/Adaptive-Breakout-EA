# Dashboards for Adaptive Breakout EA

This directory contains:

- `backend/` – FastAPI service exposing configs and (eventually) live metrics.
- `frontend/` – Placeholder for a web UI that consumes the backend API.

See individual READMEs for setup instructions.
